services:
  watchlist_hub:
    container_name: watchlist_hub
    image: watchlist_hub:latest
    restart: no
    ports:
      - "4895:4895"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
